app: FocusWriter

ingredients:
  dist: focal
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ focal main universe

  #ppas:
  #  - ppa:gottcode/gcppa

  script:
    - sudo add-apt-repository ppa:gottcode/gcppa
    - sudo apt update
    - apt list --all-versions focuswriter
    - sudo apt install focuswriter-1.8.5
    - exit
    - sudo apt install focuswriter

script:
  - set -x
  - cp ../../README.md .
